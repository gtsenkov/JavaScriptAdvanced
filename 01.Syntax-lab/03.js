function solve(num1, num2, num3){
    const max = Math.max(num1, num2, num3);

    //console.log('The largest number is ' + max + '.');
    console.log(`The largest number is ${max}.`)
}

solve(5, -3, 16);
solve(-3, -5, -22.5);